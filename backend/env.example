# Database Configuration
DATABASE_URL=postgresql+psycopg://user:password@host/database?sslmode=require

# Session Secret (generate a random secret key, minimum 32 characters)
SESSION_SECRET=your-very-secret-key-here-minimum-32-characters-change-this-in-production

# Server Port (optional, default 6543)
PORT=6543

# CORS Allowed Origins (comma-separated)
# For production, set your frontend domain(s)
# Example: CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
CORS_ORIGINS=http://localhost:5173,http://localhost:5174
